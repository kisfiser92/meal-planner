name: Deploy game score counters to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'frontend/jolly.html'
      - 'frontend/cornhole.html'
      - 'frontend/games-index.html'
      - '.github/workflows/deploy-jolly-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - name: Prepare site
        run: |
          mkdir -p site
          cp frontend/games-index.html site/index.html
          cp frontend/jolly.html site/jolly.html
          cp frontend/cornhole.html site/cornhole.html
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - id: deployment
        uses: actions/deploy-pages@v4
